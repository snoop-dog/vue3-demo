import { ref, defineComponent } from "vue"
import Router from '../routes/index'

export default defineComponent({
	setup(props, ctx) {
		// Router.go(-1)
	},

	render() {
		return (
			<div>qqq</div>
		)
	}
})
